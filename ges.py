exec(open('util.py').read())
def ges(inp):
	ray = inp[0]
	for a,val in enumerate(ray):
		fir  = val[0]
		if fir==mat:
			for b,val2 in enumerate(val[1]):
				ray = val[1]([ray])
			for b,val2 in enumerate(val[2]):
				ray.append(val2)

	run3(ray)	


inp = []
ges(inp)